#include "main.h"
#include <string.h>

/**
 * _strcmp -  function that compares two strings.
 *
 * @s1: first sring to be compared
 * @s2: second string to be compared
 *
 * Return: returns n to *ptr
 */

int _strcmp(char *s1, char *s2)
{
	int n = strcmp(s1, s2);

	return (n);
}
