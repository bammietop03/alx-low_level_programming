#include <stdio.h>
/**
 * main - execute the function which prints the value in printf
 *
 * retun: return 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	retun (0);
}
