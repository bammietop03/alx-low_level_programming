#include "main.h"
/**
* mul - multiply two integers
* @a: is the first parameter that takes the argument
* @b: is the second parameter that takes the argument
*
* Return: c
*/

int mul(int a, int b)
{

	int c;

	c = a * b;
	return (c);
}
